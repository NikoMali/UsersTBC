# UsersTBC

For project run:
1. in AppSettings.developer.json file, pls insert your database server name;
2. in CMD `dotnet ef migrations add Initial`;
3. in CMD `dotnet ef database update`;
4. in CMD `dotnet run`;

Navigate to `http://localhost:4000/swagger`


